import { LightningElement, api } from 'lwc';

/**
 * Creates the Contact object. 
 * handleSubmit event handler sends the object to the parent
 */
export default class SelfServiceRequestContactDetails extends LightningElement {
    _newContactId;
}